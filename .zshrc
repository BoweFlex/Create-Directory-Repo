if [ -f ~/Projects/CreateProj/create.zsh ]; then # Change this path to match where repository is stored, save .zshrc in ~                                                                                                                                                      
    . ~/Projects/CreateProj/create.zsh # Change this path as well
fi
